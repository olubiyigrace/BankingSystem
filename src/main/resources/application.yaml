spring:
  application:
    name: BankingSystem

  datasource:
    url: jdbc:mysql://localhost:3306/grolfbank_db
    username: root
    password: Oluwafunke3060$
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true